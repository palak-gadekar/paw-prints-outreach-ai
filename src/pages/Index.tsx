// Update this page (the content is just a fallback if you fail to update the page)

const Index = () => {
  return (
    <div className="min-h-screen flex items-center justify-center bg-background">
      <div className="text-center">
        <h1 className="text-4xl font-bold mb-4 bg-gradient-hero bg-clip-text text-transparent">Welcome to FirstHello</h1>
        <p className="text-xl text-muted-foreground">Your AI-powered animal welfare campaign tool is loading...</p>
        <p className="text-sm text-muted-foreground mt-4">If you see this, there might be a routing issue.</p>
      </div>
    </div>
  );
};

export default Index;
